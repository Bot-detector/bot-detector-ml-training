[project]
name = "ml-training"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mlflow>=3.1.4",
    "notebook>=7.4.4",
    "pandas>=2.3.1",
    "pyarrow>=20.0.0",
]

[tool.uv.workspace]
members = [
    "src/binary_model",
]
